
FILE(GLOB SRCS *.cpp *.c)
FILE(GLOB HDRS *.h)

SOURCE_GROUP("" FILES ${SRCS} ${HDRS})

ADD_LIBRARY(astc
	${SRCS}
	${HDRS}
	)

IF (FTEMU_PIC)
  SET_TARGET_PROPERTIES(bt815emu PROPERTIES COMPILE_FLAGS -fPIC)
ENDIF (FTEMU_PIC)
