
FILE(GLOB SRCS *.cpp *.c)
FILE(GLOB HDRS *.h)
FILE(GLOB RSCS *.rc)

ADD_LIBRARY(mx25lemu SHARED
  ${SRCS}
  ${HDRS}
  ${RSCS}
)

SET_TARGET_PROPERTIES(mx25lemu PROPERTIES
  COMPILE_DEFINITIONS "BT8XXEMU_FLASH_LIBRARY")
