
FILE(GLOB SRCS *.c)
FILE(GLOB HDRS GL/*.h)

ADD_LIBRARY(gl3w
  ${SRCS}
  ${HDRS}
)

IF (FTEMU_PIC)
  SET_TARGET_PROPERTIES(gl3w PROPERTIES COMPILE_FLAGS -fPIC)
  TARGET_LINK_LIBRARIES(gl3w dl)
ENDIF (FTEMU_PIC)

